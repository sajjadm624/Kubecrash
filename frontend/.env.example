# KubeCrash frontend environment variables
# Copy this to .env.local for local dev overrides

# Backend API host (no protocol prefix — just host:port or domain)
# Local dev default: localhost:8000
# Production example: your-backend.railway.app
VITE_API_URL=localhost:8000
